# Per-order checkout and receipts — local verification, 2026-09-21

**51 automated tests passed**, including independent price expectations, server-side fixed single-use link creation, current explicit consent, policy archival, environment isolation, checkout/email kill switches, authenticated Wompi readback, forged signatures/amounts, untrusted webhook customer fields, declined/pending payments, legacy links, concurrent callbacks, duplicate transactions, deterministic retries and manual review outside the safe resend window. Tests use synthetic fixtures and mocked providers; no real API credential or transaction was used.

`npm run build` passed and verified immutable policy archives. Both functions bundled/imported with esbuild for Node 22. Public output excludes server code, docs, tests, node_modules, artifacts and environment files. Hosted Netlify execution and real Blobs conditional-write semantics remain to be verified in the authorized sandbox deployment.

Browser checks through Computer Use:

- Normal checkout still defaults to two, shows 200,000 COP, requires consent, and remains disabled by the production gate.
- A separate loopback-only QA server enabled the UI with a deliberately failing mock API. The browser POST contained quantity, accepted flag, versions and an attempt ID; no customer data, amount or redirect. Failure displayed recovery/WhatsApp text and restored the button and controls.
- At 320 px, document width equalled viewport width; offer three showed 250,000 COP and its discount. Refresh restored two cards and cleared consent.
- Synthetic email at 390 px and 1280 px had no horizontal overflow; product rows, conditional discount, exact total and iPhone compatibility were readable. This was browser rendering, not a real Gmail/Outlook/Apple Mail delivery test.
- Current Resend account `nilho` showed `nilho.co` as Verified. A dedicated key form was prepared with Sending access limited to nilho.co, but not submitted.

Local evidence (outside the repository):

- `/Users/juanfelipe/foco/.artifacts/commerce-review-2026-09-21/receipt-mobile-390.png`
- `/Users/juanfelipe/foco/.artifacts/commerce-review-2026-09-21/receipt-desktop-1280.png`
- `/Users/juanfelipe/foco/.artifacts/commerce-review-2026-09-21/checkout-320-per-order.png`
- `.artifacts/receipt-preview.html` generated by `npm run preview:receipt`.

No deployment, production enablement, external email, actual test payment or key creation occurred. Still required: Functions secrets, authorized sandbox deployment/transaction/email-delivery readback, real-device Safari/VoiceOver acceptance, retirement of legacy reusable links, final review and publication approval. Stock remains explicitly manual.
