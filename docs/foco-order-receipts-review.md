# Per-order checkout and receipts — local verification, 2026-09-21

**51 automated tests passed**, including independent price expectations, server-side fixed single-use link creation, current explicit consent, policy archival, environment isolation, checkout/email kill switches, authenticated Wompi readback, forged signatures/amounts, untrusted webhook customer fields, declined/pending payments, legacy links, concurrent callbacks, duplicate transactions, deterministic retries and manual review outside the safe resend window. Tests use synthetic fixtures and mocked providers; no real API credential or transaction was used.

`npm run build` passed and verified immutable policy archives. Both functions bundled/imported with esbuild for Node 22. Public output excludes server code, docs, tests, node_modules, artifacts and environment files. Hosted Netlify execution and real Blobs conditional-write semantics remain to be verified in the authorized sandbox deployment.

Browser checks through Computer Use:

- Normal checkout still defaults to two, shows 200,000 COP, requires consent, and remains disabled by the production gate.
- A separate loopback-only QA server enabled the UI with a deliberately failing mock API. The browser POST contained quantity, accepted flag, versions and an attempt ID; no customer data, amount or redirect. Failure displayed recovery/WhatsApp text and restored the button and controls.
- At 320 px, document width equalled viewport width; offer three showed 250,000 COP and its discount. Refresh restored two cards and cleared consent.
- Synthetic email at 390 px and 1280 px had no horizontal overflow; product rows, conditional discount, exact total and iPhone compatibility were readable. This was browser rendering, not a real Gmail/Outlook/Apple Mail delivery test.
- Current Resend account `nilho` showed `nilho.co` as Verified. A dedicated key form was prepared with Sending access limited to nilho.co, but not submitted.

Local evidence (outside the repository):

- `/Users/juanfelipe/foco/.artifacts/commerce-review-2026-09-21/receipt-mobile-390.png`
- `/Users/juanfelipe/foco/.artifacts/commerce-review-2026-09-21/receipt-desktop-1280.png`
- `/Users/juanfelipe/foco/.artifacts/commerce-review-2026-09-21/checkout-320-per-order.png`
- `.artifacts/receipt-preview.html` generated by `npm run preview:receipt`.

No deployment, production enablement, external email, actual test payment or key creation occurred. Still required: Functions secrets, authorized sandbox deployment/transaction/email-delivery readback, real-device Safari/VoiceOver acceptance, retirement of legacy reusable links, final review and publication approval. Stock remains explicitly manual.

## Authorized Resend setup — completed 2026-09-21

- Created `Foco Comprobantes` in Resend's `nilho` account, Sending access limited to the verified `nilho.co` domain. Existing keys were left unchanged.
- Saved `RESEND_API_KEY` on Netlify project `nilho` (nilho.co / juanfgaviriac/nilho-website), marked **Contains secret values**, with one value in the **Production** context only. Readback showed Deploy Previews, Branch deploys, Preview Server & Agent Runners and Local development empty. Masked suffix matched the created key; the secret was not printed or committed.
- Current Netlify plan locks scope selection: the secret has Builds, Functions and Runtime scope, excluding post processing. No upgrade or plan change occurred.
- Sent exactly one synthetic test to `team@nilho.co`, from `Foco <team@nilho.co>`, reply-to `team@nilho.co`, subject `[PRUEBA] Foco · comprobante de compra`. A prominent banner and plain-text preface state that this is not a real purchase or payment.
- Resend message `01a0c659-b6f4-760c-b6b7-4c7ad9143a76` returned API acceptance; dashboard readback then showed **Delivered**. Delivery is provider-confirmed, not proof the recipient opened it.
- The authorized test process received the key through a local FIFO, not a secret-bearing disk file. The FIFO was removed and the browser-side working copy cleared after storage/send verification. Synthetic test script and non-secret result are in ignored `.artifacts/resend-verification/`.

This completes key provisioning, secure storage and the email smoke test only. Wompi credentials/callback, hosted function execution, durable order storage and an end-to-end sandbox purchase are still unverified. Checkout remains gated; no deployment, real transaction or production activation occurred.

## Wompi connection preparation — 2026-09-21

The connection review caught a deployment blocker: `CONTEXT` is a build-time variable and is not automatically supplied in Functions runtime. Both handlers now pass Netlify's trusted `context.deploy.context` into the commerce runtime. A configured `CONTEXT=production` cannot bypass a missing/preview deploy context. **52 tests and the static build passed** after this correction.

References: [Netlify Functions environment variables](https://docs.netlify.com/build/functions/environment-variables/) and [Functions deploy context](https://docs.netlify.com/build/functions/api/#deploy).

Netlify project `nilho` now has `WOMPI_ENVIRONMENT=prod` for Production and `test` for the other four contexts. `FOCO_CHECKOUT_ENABLED` and `FOCO_EMAIL_ENABLED` are explicitly `false` in every context. Saved values were read back through the UI. The existing production-only Resend secret remains unchanged. Wompi's browser session is signed out; its private/public/events credentials and webhook are still not connected. No deploy or production payment was made.

## Wompi credentials and real sandbox API — completed 2026-09-21

- Used the authenticated Foco / Juan Felipe Gaviria Campo merchant session. Read existing keys; no regeneration, payout change or bank change.
- Saved `WOMPI_PRIVATE_KEY`, `WOMPI_PUBLIC_KEY` and `WOMPI_EVENTS_SECRET` as Netlify secrets. Production values exist only in Production; test values only in Deploy Previews. Each variable's masked suffix matched the source for both contexts; Branch deploys, Agent Runners and Local development were empty. Secret scope is Builds/Functions/Runtime under the existing plan. No secret was written to git or a disk file.
- Executed the actual server `createCheckout` logic with a local in-memory order store against Wompi sandbox. Created `test_2nQdIw` (1 card, 11000000 cents), `test_PCMYEN` (2 cards, 20000000) and `test_AyTDcB` (3 cards, 25000000). Independently retrieved each provider link and checked COP, merchant identity, single use, shipping collection, SKU/order mapping, redirect and one-hour expiry. Same-attempt retries returned the existing link without another provider POST.
- These are **provider contract checks only**: no payment submitted, no webhook delivered, no hosted Blobs verification and no new email. Synthetic consent was stored only in the local test process; this is not evidence of a real customer's acceptance. Ignored non-secret report: `.artifacts/wompi-verification/result.json`. The 0600 credential FIFO was removed after execution.
- Existing production and sandbox event URL fields were blank. They remain blank until corresponding endpoints are deployed and checked. Never direct events to a currently missing endpoint.
- Both public launch gates and server switches stay off. No deployment/publication or production link creation occurred; legacy reusable links remain unchanged.
- Sandbox preview delivery needs a separate Resend key because the production secret is non-readable and production-only. Prepared, but did not submit, a `Foco Pruebas` key with Sending access restricted to nilho.co. Preview deployment and that new credential await confirmation.

## Approved sandbox preparation — 2026-09-21

The owner approved the sandbox deployment, callback setup, restricted Resend key and test receipt. Created `Foco Pruebas` (`97c3bfab-d1ad-4289-a1b9-b1fe68980ade`), Sending access limited to nilho.co, and saved it only in Netlify's Deploy Previews context. Readback matched the masked suffix; the production key was preserved. No email has yet been sent with this key.

Added a sandbox recipient allowlist, conspicuous test receipt labels, a draft-only static build and a return URL restricted to the project's sandbox alias. **54 tests and the sandbox build passed.** Production source flags remain disabled. Candidate commit: `5e67111`.

Netlify CLI 27.8.0 is available through npx. It was not previously authenticated. Its official authorization page requests permission to create/manage projects in the user's teams; this additional access is awaiting explicit confirmation. No sandbox deployment, callback change or test transaction has happened yet.

## CLI authorization and first sandbox draft — 2026-09-21

- Owner explicitly authorized the official Netlify CLI. Login readback: juanfelipe@nilho.co, team Nilho; local checkout linked to existing site `1f020403-e496-47a8-9180-dc9802c6844f` (`nilho`). No new site or paid plan.
- Draft `6ab1ca15dd2f9d12cf689b7b` is ready at `https://foco-checkout-sandbox--nilho.netlify.app`. The checkout, neutral return page and immutable terms archive return HTTP 200. Browser readback verified totals 110000 / 200000 / 250000 COP and pack-only discount. Both function bundles contain the rendered policy archives.
- Invalid checkout currently returns 503 `commerce_not_configured`, exposing a context mismatch before any payment: alias deployments use runtime `branch-deploy`, even when build context is `deploy-preview`. Wompi sandbox credentials remain in Deploy Previews pending authenticated browser access to copy them to the dedicated sandbox branch. No orders/payments were created in hosted Blobs.
- A bulk env context update omitted encrypted values and cleared the Resend values. Repaired using equivalent sending-only nilho.co keys: Production `Foco Comprobantes Netlify` (`87f90992-dac3-4d0d-a314-f21ac5fcdfd0`), sandbox branch `Foco Sandbox Netlify` (`23a12081-b3af-4ccd-be48-2ee2401c687a`). Single-value PATCH readback verified secrets and production preservation. Old Resend keys have not been revoked; they are superseded, not used by this checkout.
- Netlify checkout/email project switches remain false. This task did not publish production. During testing a separate production deployment appeared: `6ab1cad36324ac0008f3e62b`, main commit `a7cd435415b29307991b274e65087001f33d0b4b`, title “Refine Foco steps and add glass app-blocking overlay”. Its production checkout config paths return 404; reconcile current main before any final checkout publication to preserve those visual changes.
- 54 tests passed. Remaining: Wompi sign-in, branch-scoped sandbox keys, redeploy, sandbox webhook, approved transaction, stored consent/policy and one delivered receipt, duplicate callback evidence.
