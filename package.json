{
  "name": "nilho-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "test": "node --test tests/*.test.mjs",
    "preview:receipt": "node scripts/preview-receipt.mjs"
  },
  "engines": { "node": ">=22" },
  "dependencies": { "@netlify/blobs": "11.1.0" }
}
